### Prepare python for populating data to the database

```shell
python -m venv myenv
source myenv/bin/activate
pip install pyyaml
```

### Populate date to the database
```shell
python populate_data.py
```